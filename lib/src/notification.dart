// lib/src/notification.dart

/// Base marker interface for all notification messages.
/// Notifications represent events that have occurred. Handlers react to these events.
abstract class Notification {
  const Notification();
}
